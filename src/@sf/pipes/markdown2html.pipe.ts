/*
Transforms Markdown text into HTML for rendering within your Angular application.
To use this pipe, you can employ a library like marked to convert Markdown to HTML.
*/

// npm install marked

// import { Pipe, PipeTransform } from '@angular/core';
// import * as marked from 'marked';

// @Pipe({
//   name: 'markdownToHtml',
// })
// export class MarkdownToHTMLPipe implements PipeTransform {
//   transform(markdown: string): string {
//     if (!markdown) return '';
//     return marked(markdown);
//   }
// }

// <div [innerHtml]="markdownText | markdownToHtml"></div>
// <!-- Assuming markdownText contains Markdown content -->
