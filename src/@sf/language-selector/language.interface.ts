export default interface Language {
  code: string; // Language code (e.g., 'en', 'es')
  name: string; // Full name of the language (e.g., 'English', 'Spanish')
  flag: string; // Path to the flag image (e.g., 'assets/flags/us.png')
}
